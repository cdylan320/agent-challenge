{
  "name": "nosana-agents-102",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:ui": "cd apps/ui && pnpm run dev -p 3000",
    "dev:agent": "cd apps/agent && tsx src/index.ts",
    "dev:mcp": "cd packages/mcp-server && tsx src/server.ts",
    "build": "echo 'Use per-app build'",
    "lint": "eslint ."
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
